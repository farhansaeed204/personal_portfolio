[project]
name = "portfolio-backend"
version = "0.1.0"
requires-python = ">=3.12"

[tool.fastapi]
entrypoint = "main:app"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["main", "agent", "knowledge", "retrieval"]